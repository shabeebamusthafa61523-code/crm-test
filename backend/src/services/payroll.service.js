export const payrollService = {
  generateMonthlyPayrollDrafts: async (month, year) => {
    console.log(`[Payroll Service] Mock generated payroll drafts for ${month}/${year}`);
    return true;
  }
};
